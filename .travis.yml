language: node_js
node_js:
  - "10"

stages:
  - install
  - test
  - lint
  - build

install:
  - npm install

test:
  - npm run test

lint:
  - npm run lint

build:
  - npm run build

jobs:
  include:
    - stage: install
      name: "Install dependencies"
    - stage: test
      name: "Run unit tests"
    - stage: lint
      name: "Run linter"
    - stage: build
      name: "Make build"

